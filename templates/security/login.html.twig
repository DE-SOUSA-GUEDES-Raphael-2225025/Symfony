{% extends 'base.html.twig' %}

{% block body %}
    <div class="max-w-md mx-auto mt-10 p-6 bg-white shadow-xl rounded-lg border border-gray-200">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">🔐 Connexion</h1>

        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <form method="post">
            <label for="email" class="block text-sm font-medium text-gray-700">Adresse email :</label>
            <input type="email" id="email" name="_username" value="{{ last_username|default('') }}" required
                   class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">

            <label for="password" class="block text-sm font-medium text-gray-700 mt-3">Mot de passe :</label>
            <input type="password" id="password" name="_password" required
                   class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">

            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 px-4 rounded-lg text-lg font-semibold hover:bg-blue-700 transition duration-300 shadow-md mt-4">
                🔑 Se connecter
            </button>
        </form>
        {% if app.user %}
            <form action="{{ path('app_logout') }}" method="post">
                <button type="submit"
                        class="w-full bg-red-600 text-white py-2 px-4 rounded-lg text-lg font-semibold hover:bg-red-700 transition duration-300 shadow-md mt-4">
                    🚪 Se déconnecter
                </button>
            </form>
        {% endif %}


        <p class="text-center text-gray-600 mt-4">
            Pas encore inscrit ? <a href="{{ path('app_register') }}" class="text-blue-600 hover:underline">Créer un compte</a>
        </p>
    </div>
{% endblock %}
